<h1>Menu</h1>

</br>
<div class="float">
	<div class="left">
		<% @foodTypes.each do |type| %>
			<b><%= link_to type.name, foods_path(:type => type.id) %></b> 
		<% end %><br/><br/>
	</div>
</div>
<div>
<% @foods.each do |food| %>
    <b><%= food.name %></b> <span class="price"><%= food.price %> €</span></br>
    <%= food.content %></br>

    <%= link_to "Show", food %>
    <%= link_to "Edit", edit_food_path(food) %>
    <%= link_to "Destroy", food, confirm: 'Are you sure?', method: :delete %></br></br>
<% end %>
<br/>
<br/>
<%= link_to 'New', new_food_path %>
</div>
