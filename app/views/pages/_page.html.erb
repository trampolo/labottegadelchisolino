<div id="left">
	<% if !@page.photo_file.nil? %>
		<%= image_tag @page.photo.url %>
	<% end %>
</div>


<div id="right">
	<%= @page.content.html_safe %>
	<% if current_user %>
	<%= link_to 'Edit', edit_page_path(@page) %>
	<% end %>
	</br>
</div>
